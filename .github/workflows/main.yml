on:
  push:
    paths:
    - 'docs/**'
    - '**.yml'
    branches:
    - master

jobs: 
  build: 
    name: Build and Deploy Documentation 
    runs-on: ubuntu-latest 
    steps: 
    - uses: actions/checkout@v2

    - name: Build docs
      uses: actions/setup-python@v1
      with:
        python-version: 3.7
      
    - name: Install dependencies 
      run: | 
        python -m pip install --upgrade pip 
        pip install mkdocs-material 

    - name: Build and push
      run: |
        /usr/bin/git config user.name ${{ secrets.GH_USER }}
        /usr/bin/git config user.email "${{ secrets.GH_MAIL }}"
        /usr/bin/git remote add gh-token "https://${{ secrets.GH_TOKEN}}@github.com/:user/:repo.git"
        /usr/bin/git fetch gh-token && git fetch gh-token gh-pages:gh-pages
        python -m mkdocs gh-deploy -v --clean --remote-name gh-token
        /usr/bin/git push gh-token gh-pages


        